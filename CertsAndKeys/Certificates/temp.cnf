[ req ]
prompt = no
distinguished_name = dn
req_extensions = req_ext

[ dn ]
C = IE
O = Group-H Security
CN = flask.chat.internal

[ req_ext ]
subjectAltName = @alt_names
keyUsage = critical, digitalSignature, keyEncipherment
extendedKeyUsage = serverAuth

[ alt_names ]
DNS.1 = flask.chat.internal
DNS.2 = localhost
IP.1 = 127.0.0.1
